<%= form_with(model: table, local: true) do |form| %>
  <% if table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :table_name, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :number_of_places %>
    <%= form.number_field :number_of_places, id: :table_number_of_places, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "form-control btn btn-outline-success mt-3" %>
  </div>
<% end %>
