<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th>Table</th>
      <th>Total</th>
      <th>Is paid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.table_id %></td>
        <td><%= order.total %></td>
        <td><%= order.is_paid %></td>
        <td><%= link_to 'Show', table_order_path(order.table, order) %></td>
        <td><%= link_to 'Edit', edit_table_order_path(order.table, order) %></td>
        <td><%= link_to 'Destroy', table_order_path(order.table, order), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
