<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_paid %>
    <%= form.check_box :is_paid, id: :order_is_paid %>
  </div>

  <div class="field">
    <%= form.label :table_id_id %>
    <%= form.text_field :table_id_id, id: :order_table_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
